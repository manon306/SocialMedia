@model SocialMedia.BLL.ModelVM.Profile.ViewProfileVM

@{
    ViewData["Title"] = "ViewProfile";
    Layout = "_NewLayout";
}
@if (ViewBag.Error != null)
{
    <div class="alert alert-danger">@ViewBag.Error</div>
}

@* 
<div class="container mt-4">
    <div class="card bg-dark text-light shadow-lg border-0 rounded-4">
        <div class="card-body text-center">
            <img src="~/Files/@Model.ProfileImagePath"
                 class="rounded-circle mb-3 border border-3 border-light shadow"
                 width="130" height="130" />

            <h3 class="fw-bold">@Model.Name</h3>
            <h5 class="text-muted">@Model.Headline</h5>
            <p class="mt-3">@Model.Bio</p>
            <p><i class="fa-solid fa-location-dot text-warning"></i> @Model.Location</p>
            <p><strong>Skills:</strong> @Model.Skills</p>

            <div class="mt-4">
                @if (User.FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value != Model.Id)
                {
                    <form asp-controller="Connection" asp-action="SendRequest" method="post" class="d-inline">
                        <input type="hidden" name="receiverId" value="@Model.Id" />
                        @Html.AntiForgeryToken()
                        <button type="submit" class="btn btn-success">Connect</button>
                    </form>


                    <a asp-controller="Message" asp-action="Chat" asp-route-userId="@Model.Id"
                       class="btn btn-outline-light">Message</a>
                }
            </div>
        </div>
    </div>
</div> *@
   <div class="container mt-4">
    <div class="card bg-dark text-light">
        <div class="card-body text-center">
            <img src="~/Files/@Model.ProfileImagePath" class="rounded-circle mb-3" width="120" height="120" />
            <h3>@Model.Name</h3>
            <h5 class="text-muted">@Model.Headline</h5>
            <p>@Model.Bio</p>
            <p><i class="fa-solid fa-location-dot"></i> @Model.Location</p>
            <p><strong>Skills:</strong> @Model.Skills</p>

              <form asp-controller="Connection" asp-action="Requests" method="post" class="d-inline"></form> 
             <button class="btn btn-success">Connect</button>
            <button class="btn btn-outline-light">Message</button>
        </div>
    </div>
</div>
